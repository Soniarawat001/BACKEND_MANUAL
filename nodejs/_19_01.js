const fs=require("fs");
const fd=fs.openSync("data.txt","w+");
// fs.writeSync(fd,"we are here");
// const buff=Buffer.alloc(50);
// const data=fs.readSync(fd,buff,0,buff.length,0);
// console.log(buff.toString("utf-8",0,data));

